# Required Reading

**O índice único da leitura obrigatória deste projeto** — inclusive das fontes que
moram fora do checkout. A coluna "Dono" (`.docs/` é do kit e some no `--upgrade`;
`docs/` é do projeto) só importa na hora de **escrever**, não de ler.

## Deste projeto

Documentos deste repositório. Seção 100% do projeto: nenhum upgrade a toca. Use
`- (none)` se não houver nenhum.

- (none)

## Fontes locais — fora do checkout

Regra não rastreável (credencial, agenda, perfil). Registre **caminho e propósito,
nunca conteúdo**. `obrigatório` reprova no `doctor`; `opcional` só avisa.

**Se este projeto envia e-mail, declare aqui o transporte dele** — o caminho do helper
e o do arquivo de credencial. `.docs/workflows/sending-email.md` manda o agente ler
esta tabela antes de enviar, e proíbe herdar o transporte de outro projeto. Uma tabela
vazia significa "este projeto não tem transporte declarado", e o agente vai perguntar
ao operador em vez de adivinhar.

| Caminho | Obrigatório | O que é |
|---|---|---|
<!-- Exemplo: | `~/caminho/para/credenciais.conf` | opcional | credenciais do X; nunca citar | -->

### Lista de destinatários

Quem recebe e-mail deste projeto, incluindo regra recorrente de CC. Escreva aqui, ou
aponte o documento onde a lista mora. **Se não existir lista, escreva que não existe** —
o agente é proibido de deduzir destinatário do e-mail que o harness reporta, que
identifica a conta logada e não o operador.

- (não declarada — pergunte ao operador antes de qualquer envio)

## Por área

Leitura escopada: só quem mexer na área precisa.

<!-- Exemplo: `docs/architecture.md` — ao tocar na camada de orquestração -->

- (none)
