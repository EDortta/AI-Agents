# Napkin Lessons Learned

- 2026-07-27: A declared reserve must reduce usable budget; a zero-use category does
  not reserve anything. Name task and risk contract costs independently so telemetry
  explains where policy tokens are spent.

- 2026-07-27: Measure canonical rules before choosing a context budget. The base is
  about 7.2k tokens and implementation adds about 8k; a 12k ceiling would weaken the
  contract. Keep mandatory documents atomic and save tokens by excluding unrelated
  roles/history rather than truncating rules.

Short, practical lessons captured at session close.
Keep each lesson concise and actionable.

## Entry format
- `[YYYY-MM-DD] <work_id> - <lesson>`
- `Action next time: <specific behavior to repeat/avoid>`

## Entries
- `[2026-05-07] WK-20260507-personal-touch-1.0.2 - Cursor ignores chain-loaded files; tool adapters must be self-contained to be effective.`
- `Action next time: Write .cursorrules to cover start gate, hard rules, session-close format, quality gates, and branch rules — no chain-loading assumption.`
- `[2026-05-07] WK-20260507-personal-touch-1.0.2 - USER.md is a global user-level file (~/.config/USER.md); never put it in the project repo or the install script.`
- `Action next time: Document the convention in README and all adapter files; keep it optional so the kit works without it.`
- `[2026-05-04] WK-20260504-low-token-contract-v2 - Keep root contracts as dispatchers and move detailed behavior to role/workflow docs to reduce repeated context.`
- `Action next time: Preserve hard gates in AGENTS.md, but push task-specific detail behind explicit load rules.`
- `[2026-05-04] WK-20260504-low-token-contract-v2 - Upgrade paths must preserve target-local context while replacing managed directories so removed kit files disappear.`
- `Action next time: Test fresh install and upgrade separately before declaring installer behavior safe.`
- `[2026-05-11] WK-20260511-php-delphi-audit-capability - When adding language support to the kit, mirror the exact output format of the existing reference (typescript-audit.md) — teams can then compare maturity scores across languages on the same scale.`
- `Action next time: Always produce the new audit workflow file first, then update programmer.md and reviewer.md; the workflow file is the source of truth that informs what rules belong in the contracts.`
- `[2026-05-11] WK-20260511-php-delphi-audit-capability - A real audit run (YeAPF2, 86 files, PHP 5.5/10) revealed that tooling baseline (PHPStan, CS-Fixer) is the single highest-leverage item: installing it costs 1 hr and gates all other type-safety improvements.`
- `Action next time: Lead audit recommendations with tooling setup, not code changes — without PHPStan, devs have no feedback loop to sustain improvements.`
- `[2026-07-01] WK-20260701-dotdocs-kit-layout - A path sweep by prefix (docs/agents etc.) misses bare directory args in shell examples (cp -r AI-Agents/docs) and links prefixed with ./ that a negative-lookbehind guard skips; adversarial skeptics caught 3 such stragglers in tutorials.`
- `Action next time: After a mechanical rename sweep, run a second grep for the bare token (word 'docs' as a path arg, './docs', 'AI-Agents/docs') — not just the prefixed forms — and verify with an independent reviewer.`
- `[2026-07-01] WK-20260701-dotdocs-kit-layout - A migration that auto-promotes files must never claim 'complete' when conflicts strand items, and must never rm -rf an existing backup.`
- `Action next time: Track a conflict counter, print an honest finished-with-N-conflicts message, and pick a free backup name (bak, bak-1, ...) instead of clobbering.`
- `[2026-07-02] WK-20260702-branch-ascii-and-identity - A branch named "development" (quotes part of the ref) corrupted tooling because issue titles were passed near-raw to git; contracts had no character allow-list.`
- `Action next time: Whenever a helper can create a ref, validate against ^[a-zA-Z0-9/_-]+$ before touching git, and document the same rule in AGENTS.md so agents sanitize slugs before checkout -b.`
- `[2026-07-02] WK-20260702-branch-ascii-and-identity - Shared governance docs on a shared branch hide host-level collisions (two hosts commit on the same branch, ports clash) because nothing individualizes the instance.`
- `Action next time: Mandate a per-instance identity file (operator/host/paths/ports/branch_ownership) read before acting, with a same-branch guard, and split shared vs individual artifacts explicitly.`
- `[2026-07-07] WK-20260707-sec-standards-hardening - ~296 catalogued vulns + 11 per-project SECURITY-ALERTs + napkin lessons across kit projects surfaced ~13 recurring classes the 8-section security-standards did not explicitly name (path-traversal/SSRF, SQL/shell injection, disabled TLS verify, weak crypto/token lifecycle, fail-open authz, secrets/PII in URLs/logs/synced dirs, mutable-ref supply chain, commit-only enforced by prompt goodwill only, prompt-injection auto-actions).`
- `Action next time: Harvest ecosystem SEC-* + napkin lessons, classify against the current standards sections, and open ONE epic (docs/issues) with a task per gap that proposes concrete rule text + notes doctor-automatability — do not edit the standard directly; let the operator approve each rule. Kit files must use [OPERATOR_NAME], never a real name (the SEC-0102 anti-reintroduction gate this epic itself codifies).`
- `[2026-07-08] WK-20260708-deploy-gw-hub-vm - Dois planos do mesmo alvo (192.168.7.200) divergiam em topologia (1 VM vs 3 LXCs) E um deles se auto-contradizia: a intro dizia "caixa ociosa, operador aprovou wipe" mas a própria seção de descobertas listava infra VIVA (nginx :80 servindo /enviar-arquivo/, túnel :2203, OpenVPN, DHCP) com gate de limpeza ainda aberto.`
- `Action next time: Antes de gerar issues de deploy, ler os planos companheiros por inteiro e cruzar a intro com as seções de descobertas — a premissa do topo pode estar desatualizada. Escolher a topologia ADITIVA (VM isolada) quando ela evita reabrir um gate destrutivo, e escrever a lista de infra-intocável como pré-flight explícito na issue de runbook, não só na cabeça.`
- `[YYYY-MM-DD] WK-YYYYMMDD-example - <lesson learned>`
- `Action next time: <what to do differently>`
- `[2026-07-16] WK-20260716-ai-issues-sweep - Uma issue afirmava "lógica testada por unit (start/end/underflow/guard)" e o repositório não tinha teste nenhum — nem suíte, nem pytest, nem tests/. A afirmação falsa é pior que a ausência: aposentou o risco na cabeça de todo leitor seguinte, inclusive na minha, até eu ir olhar.`
- `Action next time: Antes de escrever "testado" em issue/commit/handoff, nomear o arquivo e mostrar a execução; caso contrário escrever "not validated: <o quê>". Reviewer trata claim-sem-arquivo como BLOCKER (design-standards.md §1).`
- `[2026-07-16] WK-20260716-ai-issues-sweep - O guard in-flight foi posto no chamador (um ramo do watchdog) enquanto o SIGKILL morava em _kill_stale_chrome(); e o reaper poupava só o PID pai do Chrome. O processo que o log da issue mostra morto (pid=3568219 age=376s cpu=37.9%) era um renderer FILHO — velho, quente e oculto, os três critérios de kill. O guard escondia o sintoma; a proteção não cobria o que dizia cobrir.`
- `Action next time: Guard vai DENTRO da operação perigosa, não ao lado do chamador que hoje sabe dele. E quando a proteção fala de "um processo", perguntar se ela cobre a árvore — renderers são filhos.`
- `[2026-07-16] WK-20260716-ai-issues-sweep - No cache de markdown, store() era à prova de falhas e lookup() não: um erro de leitura derrubaria a requisição que o cache existe para acelerar. Robustez assimétrica lê como robusta e não é.`
- `Action next time: Promessa transversal ("nunca levanta", "nunca quebra a request") vale em TODOS os pontos de entrada do módulo, não só no que foi revisado. Escolher a direção do fail de propósito: fechado para auth/segredo, aberto para cache/telemetria.`
- `[2026-07-16] WK-20260716-git-remote-bare-self-hosted - O installer do kit tem DOIS caminhos de cópia (upgrade na linha ~312 e instalação nova na ~366). Adicionar o script novo só no primeiro fazia uma instalação limpa não receber o gbr — e o teste de instalação foi o que pegou.`
- `Action next time: Ao adicionar arquivo distribuído pelo kit, grepar o installer pelo vizinho (agent-worktree.sh) e cobrir TODAS as ocorrências; depois rodar o installer de verdade num alvo limpo E um --upgrade sobre ele.`
- `[2026-07-17] WK-20260717-solid-council - Completar um framework conhecido (as letras faltantes de SOLID) empurra para criar seção por letra. Mas a Provenance é POR SEÇÃO: OCP como §8 obrigaria o chrome_op_guard() morto a aparecer na Provenance de §7 E §8 — um incidente lendo como dois — ou §8 nasceria sem provenance, que é a decoração que o §0 condena. Inflar evidência num arquivo cuja tese é honestidade é pior do que a lacuna.`
- `Action next time: Ao acrescentar regra a um arquivo com Provenance, achar primeiro QUAL incidente já registrado a ancora, e pôr a regra na seção desse incidente. Se nenhum ancora, a regra é preventiva: entra como IMPROVEMENT e a Provenance diz "preventiva, sem incidente; candidata a remoção se nunca pegar nada". Nunca MANDATORY sem incidente.`
- `[2026-07-17] WK-20260717-solid-council - O kit tinha um instrumento que funcionou e sumiu: 3 adversarial skeptics na epic 002 renderam 6 findings, todos corrigidos e retestados — e viraram nota de rodapé no handoff, nunca contrato. Um instrumento sem contrato não roda de novo; ele vira anedota que prova que o trabalho era bom.`
- `Action next time: Quando uma técnica ad-hoc render resultado real, o session-close pergunta "isto vira contrato?" antes de virar só evidência retroativa no handoff. E ao contratualizá-la, achar o arquivo vizinho que trata o caso OPOSTO (aqui: governance-precedence trata papéis que discordam; o concílio trata ninguém discordar) e escrever a fronteira em tabela — senão os dois brigam pelo mesmo gatilho.`
- `[2026-07-17] WK-20260717-solid-council - Escrevi um contrato (council.md §4) cujo trigger MANDATORY "mudança em contrato kit-owned que propaga para outros repos" descreve exatamente o épico que o estava criando. O contrato pede um concílio sobre si mesmo, e eu não rodei.`
- `Action next time: Depois de escrever um gate, reler os próprios triggers contra o trabalho em curso ANTES do session-close — se o gate novo se aplica ao commit que o introduz, ou roda, ou a exceção fica escrita no handoff. Um gate que o próprio autor pula na estreia ensina que ele é opcional.`
- `[2026-07-23] WK-20260723-agents-md-protegido - O kit já tinha a proteção certa (sync_dir: manifesto + sha256 + stash em .gk/overwritten/) e ela não alcançava os arquivos de RAIZ, que passam por copy_file_replace — um cp -a seco. Passei a issue inteira achando que ia projetar um mecanismo; o trabalho real era ligar o mecanismo existente num segundo caminho de código.`
- `Action next time: Antes de desenhar proteção nova, grepar o repositório pela proteção que já resolve o caso vizinho. Dois caminhos de cópia no mesmo instalador já mordeu antes (WK-20260716: gbr só no caminho de upgrade) — quando um arquivo tem mais de um caminho de escrita, a pergunta é sempre "os dois foram cobertos?".`
- `[2026-07-23] WK-20260723-agents-md-protegido - A proteção funcionava e o write_manifest a desfazia: ele grava o hash do que está em disco, e o disco tem a versão do PROJETO quando o arquivo é preservado. O manifesto passaria a dizer "conteúdo intocado do kit" e o upgrade SEGUINTE apagaria tudo. Perda idêntica, um turno depois — e o teste de um upgrade só passa verde.`
- `Action next time: Ao adicionar um caminho "preserva em vez de escrever", perguntar o que o registro de estado grava depois dele. E testar a operação DUAS vezes seguidas: proteção com estado só se prova no segundo ciclo, nunca no primeiro.`
- `[2026-07-23] WK-20260723-agents-md-protegido - O --check reportou "No drift" em 20 alvos reais, incluindo o jk-structure que MOTIVOU a issue. Ele iterava as chaves do manifesto; arquivo ausente do manifesto era invisível — enquanto o upgrade trata ausência como fail-closed e preserva. Relatório e comportamento discordavam, e o relatório era o otimista: dizia "pode subir" sobre exatamente os alvos onde o upgrade pararia. Causa raiz: a lista de arquivos de raiz existia em três lugares e uma delas divergiu.`
- `Action next time: Um relatório dry-run tem que percorrer a MESMA lista e a MESMA tabela de decisão que a operação real — de preferência a lista literal compartilhada, não uma reconstrução. E quando o dry-run contradiz o que a issue afirma sobre um alvo conhecido, o suspeito é o dry-run, não a issue: ir olhar o alvo antes de acreditar no verde.`
- `[2026-07-23] WK-20260723-agents-md-protegido (Parte 3, planejamento) - Ia construir uma allowlist de tokens para distinguir o placeholder [OPERATOR_NAME] do vocabulário de conteúdo [MANDATORY]/[PROHIBITED]/[DEFAULT], que compartilham a forma [MAIÚSCULA]. O operador propôs mudar o delimitador para {{TOKEN}}; verifiquei que {{ e ${{ não ocorrem no kit. A ambiguidade que exigia a allowlist simplesmente evaporou — {{...}} é sempre slot, [...] é sempre conteúdo. Detecção vira sintática, sem lista para manter.`
- `Action next time: Quando a detecção precisa de uma allowlist para separar sinal de ruído que TÊM a mesma sintaxe, perguntar antes se dá para trocar o delimitador e deixar a própria sintaxe carregar a distinção — costuma ser mais barato e mais robusto que manter a lista. Confirmar que o novo delimitador tem namespace vazio (grepar por ele e pelas variantes vizinhas, ex. ${{ do GitHub Actions) antes de adotar.`
- `[2026-07-24] WK-20260723-agents-md-protegido (Parte 3) - Adotei {{...}} como sintaxe de slot e escrevi, na própria prosa que explica a convenção, o token genérico {{TOKEN}} literal. Isso teria (a) sido substituído junto com os slots de verdade, colocando o nome real do operador exatamente no parágrafo que manda mantê-lo fora dos arquivos rastreados, e (b) feito o grep do Start Gate acusar o arquivo para sempre. Só apareceu porque rodei o cenário 1 num alvo sintético em vez de confiar no bash -n.`
- `Action next time: Documentação de um mecanismo de substituição não pode escrever o padrão substituível literalmente — usar uma grafia que o motor comprovadamente não casa ({{…}} com reticências Unicode, aqui) e travar isso com uma checagem, porque a próxima pessoa vai escrever o literal de novo por ser mais legível. Vale para qualquer template engine, não só este.`
- `[2026-07-24] WK-20260723-agents-md-protegido (Parte 3) - Depois do --migrate, o arquivo É a versão renderizada do kit, mas o manifesto ainda guardava o hash pré-migração — e o copy_file_replace, que julgava só pelo manifesto, marcava como deriva um arquivo byte a byte idêntico ao que estava prestes a escrever, pedindo merge do arquivo contra ele mesmo. A regra que faltava era trivial: dst == src, não há o que preservar nem o que reportar.`
- `Action next time: Quando a decisão depende de um registro de estado (manifesto, lockfile, cache de hash), comparar ANTES com a realidade que está à mão — se origem e destino são idênticos, o registro é irrelevante e não deve poder produzir um veredito. Registro de estado envelhece; comparação direta, não. E a regra tem que entrar no dry-run e na operação real ao mesmo tempo, senão eles voltam a discordar.`
- `[2026-08-04] WK-20260804-session-memory - O instalador copiava o handoff.md (31 KB) e o napkin-lessons.md (15 KB) DESTE repositório para dentro de cada alvo, com o histórico de sessão e os nomes dos projetos de onde cada lição veio. É a mesma falha que o kit já tinha corrigido para o README.md — e ninguém a procurou nos vizinhos do README. Lição: quando um arquivo do kit vaza para os alvos, varrer a lista inteira de _FRESH_PATHS na mesma passada, não só o arquivo que doeu.`

- `[2026-08-06] WK-20260806-council-commit-gate — primeira rodada de council registrada sob o contrato de council.md (§4 pedia isso desde 2026-07-01; zero rodadas até hoje). 3 lentes, 3 achados sobreviventes, 3 fechados com correção no mesmo commit, 3 perguntas em aberto. Os três achados foram do mesmo formato: um índice/lista atualizado num lugar e não no outro (write_gk_gitignore no shell vs no Python; docs/required-reading.md vs .docs/agents/README.md), e uma afirmação de contrato sem artefato atrás (o "pre-commit reprova" que dependia de install-hooks, que é opt-in).`
- `Action next time: quando a entrega altera um índice, listar TODOS os índices do mesmo fato antes de commitar — o council achou dois pares divergentes numa entrega que eu considerava completa. E toda frase do contrato que promete que algo "reprova" precisa apontar o comando que o instala, senão a promessa vale só onde alguém lembrou de rodá-lo.`
- `[2026-08-06] WK-20260806-council-commit-gate — o pre-commit que o kit instala NUNCA reprovou nada pelo doctor, por dois defeitos somados: o hook chamava python3 -m governancekit.cli, e cli.py não tinha guarda __main__ (importava, não imprimia, saía 0); e o veredito era lido por python3 - <<'PY', onde o heredoc SUBSTITUI o pipe como stdin. Os dois sobreviveram porque tests/test_hooks.py só afirmava sobre o TEXTO do script — "a string blocked está no arquivo" — que passa para um hook que nunca bloqueia.`
- `Action next time: teste de hook, de script gerado, ou de qualquer coisa que só existe para rodar, tem que EXECUTAR. Asserção sobre o conteúdo do arquivo prova que o texto foi escrito, não que o mecanismo funciona. E vale checar por mutação: desligue o mecanismo e confirme que o teste falha — foi assim que este passou a valer alguma coisa.`
- `[2026-08-06] WK-20260806-council-commit-gate — o gatilho `not-validated` do gate novo casava por substring e disparou sobre uma frase do napkin que ENSINA o marcador. É a terceira ocorrência do mesmo defeito neste kit: o _check_ready_flag casava a prosa do template que explicava o flag, e a lição de 24/07 é sobre documentar um mecanismo de substituição escrevendo o padrão substituível literalmente. Três vezes, três lugares, mesma forma.`
- `Action next time: todo detector de marcador em documento nasce ancorado — ^ com marcador de lista/citação opcional, nunca substring — e nasce com os dois testes: a prosa que fala do marcador NÃO dispara, a linha real dispara. O texto que descreve um padrão vive no mesmo repositório que o padrão; presumir que não vive é o que reintroduz isto.`
- `[2026-08-06] WK-20260804-governancekit-contract-reassessment (crítica A1) - Marquei a A1 como "fechada na prática, verificada no CodexBridge instalado". O que eu tinha verificado é que os ARQUIVOS estão em docs/; não verifiquei que o TEXTO DO GATE aponta para docs/. O AGENTS.md de lá, reescrito no mesmo dia, ainda nomeia .docs/ — o gate segue inoperante. Pior: o critério de aceitação da própria issue é um grep no repositório da FONTE, que passa enquanto o parque continua quebrado. A correção só chega ao alvo por uma cadeia de quatro elos (fonte → tag → DEFAULT_REF+checksum no OUTRO repo → upgrade), e o elo 3 nem sequer é do repositório onde a issue foi escrita.`
- `Action next time: critério de aceitação de defeito que aparece em projeto governado se verifica NO PROJETO GOVERNADO, nunca na fonte. E antes de fechar, desenhar a cadeia inteira até o alvo e nomear o dono de cada elo — se algum elo mora em outro repositório, a issue não fecha sozinha e isso tem que estar escrito nela.`
- `[2026-08-06] WK-20260804-governancekit-contract-reassessment (crítica B1) - A B1 propõe uma seção de gates por classe de ação, começando por e-mail. A §3b já dizia, [MANDATORY], "No external effect without explicit confirmation. Sending a message (WhatsApp, e-mail, push, webhook) ... is never fired on the agent's own initiative" — desde 27/07, oito dias ANTES do incidente, e presente no AGENTS.md do projeto onde ele aconteceu. O agente leu o arquivo inteiro e enviou. E o council.md tinha cinco gatilhos [MANDATORY] em prosa: cinco semanas, zero rodadas; o que o fez rodar foi mover a detecção do gatilho para dentro da ferramenta e o bloqueio para o pre-commit.`
- `Action next time: antes de propor regra nova em prosa, grepar o contrato pela regra que já cobre o caso — se ela existe e o incidente aconteceu assim mesmo, o problema não é a ausência da regra e escrever outra não conserta. E ao citar a §8c como modelo, lembrar que ela é ancorada na RESPOSTA (incondicional, sem reconhecer nada), não na ação: um gate que começa com "quando você for fazer X" tem como pré-condição o agente perceber X, e perceber X falha junto com lembrar da regra.`
- `[2026-08-06] WK-20260806-council-commit-gate - O gate novo disparou no MEU session-close e não podia ser satisfeito: o gatilho not-validated lê o ARQUIVO handoff.md inteiro, não as linhas ADICIONADAS pelo diff staged. O marcador estava em handoff.md:138, entrada de 27/07 — dez dias antes — e o meu diff adiciona zero ocorrências. Consequência: uma vez que qualquer entrada passada carregue o marcador, TODO commit que toca handoff.md dispara o gate para sempre, e a única saída é o waiver. Um gate que não pode ser satisfeito pelo trabalho corrente ensina a waivar por reflexo, que é como ele morre.`
- `Action next time: gatilho que fala sobre "a entrega" tem que ler o DIFF (linhas adicionadas), nunca o arquivo — arquivo acumula histórico e o gate passa a julgar o passado. É a quarta forma do mesmo defeito de detecção neste kit (substring vs âncora, prosa que ensina o marcador, e agora escopo arquivo vs diff): a pergunta a fazer sempre é "sobre QUAL texto este detector decide?".`
- 2026-08-07: WK-20260807-not-validated-trigger-scope — o gatilho `not-validated` errava por escopo, e o §4 deste contrato já dizia o escopo certo ("the delivery's `Tests` section"); a ferramenta tinha largado as duas qualificações. Mas a leitura literal do §4 também estava errada: contra os `handoff.md` reais, exigir uma seção `Tests` perde três das quatro ocorrências, porque entradas escritas como lista simples não têm subseção. O texto foi reescrito para dizer o que a ferramenta faz e por quê. Lição: quando a ferramenta e o contrato divergem, nem sempre a ferramenta é a errada — mas nunca se corrige uma sem reler a outra, senão o código passa a implementar em silêncio algo mais estreito do que está escrito.
- 2026-08-07: WK-20260807-not-validated-trigger-scope — o §4 pedia o gatilho "on a runtime path". Nenhuma versão do gate implementou isso, e nenhuma pode: um detector de linha não julga sobre o QUE uma reivindicação fala. As palavras saíram do contrato em vez de continuarem prometendo uma distinção que a ferramenta não faz. Lição: qualificador que nenhum mecanismo consegue avaliar não é requisito, é decoração — e decoração num `[MANDATORY]` ensina que `[MANDATORY]` é aproximado.
- 2026-08-07: A3 — o `install-agents-kit.sh` escrevia `.gk/.gitignore` e `.credentials/.gitignore` e nunca tocava no `.gitignore` do projeto, por decisão explícita ("o instalador nunca precisa editar o .gitignore do projeto"). O custo era invisível até alguém rodar o `doctor`: projeto instalado pelo shell nascia reprovando o gate obrigatório de segredos, com uma mensagem mandando rodar um comando Python que ele nunca tinha executado. Lição: uma regra de `.gitignore` para caminho raiz só pode morar no `.gitignore` raiz — quando a decisão de não tocar num arquivo torna um gate insatisfazível, a decisão é que está errada, não o gate.
- 2026-08-07: A3 — o instalador tem DOIS pontos onde escreve os arquivos de estado (instalação nova e o caminho de migração/upgrade), e liguei a função nova em um só. É exatamente o precedente que dá nome à lente *second caller* neste kit, repetido pelo agente que estava usando a lente. Lição: ao acrescentar uma escrita ao instalador, `grep` do nome da função irmã e conferir a CONTAGEM de chamadas antes de dar por ligado — o segundo caminho não aparece no teste feliz.
- 2026-08-07: B3 — a seção `Fontes locais` do índice mora FORA do bloco gerenciado, porque `docs/` é território do projeto. Consequência que o council obrigou a encarar: nenhum projeto já instalado vai receber a seção por `--upgrade`, nunca. A saída não foi quebrar a regra de propriedade — foi fazer o advisory do `doctor` dizer COMO criar a seção, e ele dispara exatamente quando ela faz falta. Lição: quando a regra de propriedade impede a entrega, o veículo é o aviso acionável, não uma exceção na regra — exceção em regra de propriedade é como o `--upgrade` volta a invadir `docs/`.
- 2026-08-07: B3 — o orçamento de contexto (`base_contracts`, 8000) recusou a seção nova três vezes seguidas e me obrigou a cortar prosa até 7999. Foi o gate funcionando: o texto final é melhor que o primeiro. Mas o contrato-base agora está no teto — medido em 7.238 em 27/07 — e a próxima issue que tocar o `AGENTS.md` bate na parede. Lição: levantar o teto para o teste passar é o movimento que transforma orçamento em decoração; quando o teto aperta de verdade, isso é decisão do operador, com o motivo escrito, não um ajuste de quem está com pressa.
- 2026-08-07: o `AGENTS.md` é lido INTEIRO por todo agente em toda sessão de todo projeto governado, e tinha voltado a 25.5 KB — 38% de crescimento em catorze dias, com o orçamento de contexto em 7.999/8.000. É a SEGUNDA vez que o mesmo arquivo precisa do mesmo remédio (`3cf14c7`, 04/05, cortou 68%); no meio, `7cb01fa` tentou compactar prosa e rendeu 683 B. Lição: contrato-base cresce por acréscimos individualmente defensáveis, e compactar prosa não reverte isso — o que reverte é mover assunto inteiro para trás do índice. Só 62% do arquivo é de fato incondicional ("antes de qualquer ação", "toda mensagem", "no início de cada resposta"); o resto tem gatilho e não precisa estar residente.
- 2026-08-07: fatiamento (council) — escrevi um check novo para garantir que todo `§N` resolve, e ele agrupava TODOS os headings do parque num namespace único. Como número de seção é inteiro pequeno, `§9` casava com o `## 9` de `security-standards.md` e de três auditorias: o check respondia "§9 existe em algum lugar?", que é sempre sim. Simulando a remoção do alvo real, ele seguia verde. Lição: verificador de referência resolve contra o ARQUIVO citado, nunca contra um conjunto global — e a prova de que ele funciona é apagar o alvo e ver o check ficar vermelho, não vê-lo passar.
- 2026-08-07: fatiamento (council) — o stub que substituiu a regra de deploy autônomo (a única com incidente real registrado) saiu SEM a cláusula "as regras valem com o arquivo aberto ou não", enquanto o stub de gates de qualidade, muito menos crítico, saiu com ela. Fraseado inversamente proporcional ao risco, sem ninguém decidir isso. Lição: ao substituir regra por ponteiro, ordenar os ponteiros por consequência e escrever o mais forte primeiro — o reforço verbal tem de acompanhar o risco, não a ordem em que o texto foi cortado.
- 2026-08-07: fatiamento (council) — o `--upgrade` copiava o `AGENTS.md` ANTES de sincronizar `.docs/workflows/`. Enquanto o contrato era autocontido isso era inócuo; a partir do momento em que ele aponta para arquivos daquele diretório, uma interrupção nessa janela deixa um contrato citando arquivos que não existem. Lição: quando um arquivo passa a depender de outro, a ordem de escrita do instalador vira parte do contrato — alvo primeiro, ponteiro depois, para que a pior interrupção deixe o estado anterior íntegro.
- 2026-08-10: WK-20260810-sending-email-canonico (council r1) — a varredura que aposentou a prescrição de transporte procurou por "Sending Email" e não encontrou o `## Enviar e-mail`, o stub de roteamento no `AGENTS.md` que dizia a MESMA regra em português. Pior que um straggler comum: o arquivo novo abre com "se conflitar com /AGENTS.md, siga /AGENTS.md", então a regra aposentada continuou VENCENDO a substituta em todo projeto instalado. Lição: varredura de contrato multilíngue casa pelo FATO (aqui, o caminho `~/.config/email`), nunca pelo título da seção — título traduz, caminho não. E quando o arquivo que você corrige defere a outro, corrija o outro primeiro, senão o defeito sobrevive com precedência.
- 2026-08-10: WK-20260810-sending-email-canonico (council r1) — para cumprir "cada projeto declara o seu transporte", escrevi essa declaração no `docs/required-reading.md` DESTE repositório. O `sync_reading_index` semeia alvo novo copiando esse arquivo: todo projeto instalado passaria a declarar, como seu, o helper de e-mail do operador do kit. A receita entregando a doença. O kit já tinha o remédio para `handoff.md` e `napkin-lessons.md` — semear de template — e ninguém olhou o vizinho. Lição: antes de escrever qualquer fato específico do projeto num arquivo do kit, perguntar "este arquivo é semeado nos alvos?" — e é a segunda vez que a resposta pega alguém aqui de surpresa (04/08, mesma varredura de `_FRESH_PATHS`).
- 2026-08-10: WK-20260810-sending-email-canonico (council r2) — a asserção que escrevi para impedir a recorrência do achado #1 PASSAVA contra o defeito que existia para pegar: o regex exigia um caractere depois de `email/` e a linha ofensora fechava com crase. E, já corrigida, ainda varria só `AGENTS.md` e `.docs/*` — a mesma regra colada no `CLAUDE.md` deixava tudo verde. Duas formas do mesmo erro na mesma asserção, uma por rodada. Lição: teste de recorrência se prova rodando contra a árvore ANTES da correção, arquivo por arquivo do conjunto que ele diz cobrir — "ficou verde" prova que ele não quebrou, não que ele vê. E o conjunto certo aqui é o mesmo que a ferramenta vizinha varre, não o que a memória lembra.
- 2026-08-10: WK-20260810-sending-email-canonico (council r2) — o back-fill da seção `Fontes locais` casava UMA grafia exata; o `doctor` aceita uma família inteira e a dica de remediação dele recomenda outra grafia. Num projeto que tinha seguido a dica do doctor, o `--upgrade` acrescentava uma segunda seção, vazia, terminando em "(não declarada — pergunte ao operador)" poucas linhas abaixo da lista de CC real. Duas respostas contraditórias para a pergunta cuja corrupção abriu a issue. Lição: quando duas ferramentas do mesmo kit decidem sobre o mesmo heading, uma delas não pode usar `grep -F` — a detecção mora numa origem só, ou a mais estrita corrompe o que a mais frouxa aceitou. É a quinta forma do defeito de detecção neste kit.
- 2026-08-10: WK-20260810-sending-email-canonico (council r2) — o scaffold que o kit passou a semear tem cabeçalho de tabela e nenhuma linha de dado, e o `_parse_local_sources` do doctor lê isso como "tem tabela mas nenhuma linha legível": todo projeto novo nasce com um HINT acusando de malformada uma tabela que o próprio kit escreveu. Alvos que antes da entrega estavam verdes. Lição: ao passar a SEMEAR uma estrutura, rodar o `doctor` no alvo semeado antes de commitar — o gate do repositório-fonte não vê o que nasce no alvo, e uma entrega que ensina o operador a ignorar advisory custa mais do que o defeito que ela corrige.
- 2026-08-11: WK-20260811-version-chain-coordination (council r1) — para bumpar o GovernanceKit de `0.2.3` a `0.3.0` eu ia publicar o range `>=0.3.0,<0.4.0` neste contrato, porque é o range semanticamente correto. A lente *migrator* perguntou o que acontece com quem tem só um dos dois lados atualizado, e a resposta estava em `doctor.py:239`: `blocking = config is not None and config.project_state == "existing"`. Para projeto `existing`, `incompatible` não é advisory — é `[FAIL]` duro, e o §8b transforma isso em STOP. Medido no parque: 29 projetos têm `.gk/manifest.json`, 4 têm `project-config.json` com `existing`, e os 4 declaram `>=0.2.2,<0.3.0` sobre um kit `v1.1.6`. Um deles é o `jk-structure`, com trabalho vivo. O range correto teria parado agente em produção para publicar um número de versão. Lição: antes de estreitar um range de compatibilidade, contar os projetos que o declaram HOJE e conferir por qual caminho o `doctor` reprova cada um — `advisory` e `FAIL` são a diferença entre um aviso e uma parada, e o range só pode estreitar depois que o parque já foi migrado, nunca antes.
- 2026-08-11: WK-20260811-version-chain-coordination (council r1) — a primeira varredura de `v1.2.0` filtrou por `--include=*.sh,*.json,*.md,*.py` e deu por completa. Faltavam 8 arquivos: as quatro landing pages, cada uma em duas cópias (`docs/` e `.docs/`). Quem pegou foi o §10d do `run-checks.sh`, não eu. Lição: numa troca de ref de release, a varredura é `grep -rn` sem `--include` nenhum, com a exclusão explícita do que é histórico (`handoff.md`, `docs/issues/`) — a lista de extensões é uma hipótese sobre onde a versão aparece, e a versão aparece onde a documentação for publicada.
